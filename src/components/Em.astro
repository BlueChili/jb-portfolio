---
interface Props {
        variant?: 'light' | 'dark'
        class?: string;
}

const variant = Astro.props.variant || 'light';

const textColor = variant === 'light' ? 'text-black' : 'text-white';
---
<em class='not-italic' class:list={[ textColor, Astro.props.class ]}><slot/></em>
